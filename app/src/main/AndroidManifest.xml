<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission
        android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:screenOrientation="portrait"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_title_login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screens.HomeActivity"
            android:label="@string/app_title_home"
            android:exported="true">
        </activity>
        <activity
            android:name=".screens.IndexCardsActivity"
            android:label="@string/app_title_indexcards"
            android:exported="true">
        </activity>
        <activity
            android:name=".screens.EventsActivity"
            android:label="@string/app_title_events"
            android:exported="true">
        </activity>
        <activity
            android:name=".screens.HomeworksActivity"
            android:label="@string/app_title_homeworks"
            android:exported="true">
        </activity>
    </application>
</manifest>